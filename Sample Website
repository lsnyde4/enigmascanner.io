<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>IT 304 Group 8 – AI Image Detector</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    /* ---- Base styles ---- */
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      color: #222;
      background: #f5f5f7;
      line-height: 1.5;
    }

    img {
      max-width: 100%;
      display: block;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    .container {
      width: 100%;
      max-width: 1100px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }

    /* ---- Header / Nav ---- */
    header {
      background: #ffffff;
      border-bottom: 1px solid #e3e3e3;
      position: sticky;
      top: 0;
      z-index: 10;
    }

    .nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.75rem 0;
    }

    .logo {
      font-weight: 700;
      font-size: 1.2rem;
    }

    .logo span {
      color: #2563eb; /* primary accent color */
    }

    .nav-links {
      display: flex;
      gap: 1.25rem;
      font-size: 0.95rem;
    }

    .nav-links a {
      opacity: 0.8;
    }

    .nav-links a:hover {
      opacity: 1;
    }

    .nav-actions {
      display: flex;
      gap: 0.75rem;
      align-items: center;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.45rem 0.9rem;
      border-radius: 999px;
      border: 1px solid transparent;
      font-size: 0.9rem;
      cursor: pointer;
      background: #2563eb;
      color: #fff;
      font-weight: 500;
    }

    .btn-outline {
      background: transparent;
      color: #2563eb;
      border-color: #2563eb;
    }

    .btn:hover {
      filter: brightness(1.03);
    }

    @media (max-width: 800px) {
      .nav-links {
        display: none; /* keep it simple; no mobile menu JS */
      }
    }

    /* ---- Hero ---- */
    .hero {
      padding: 3rem 0 2.5rem;
      background: radial-gradient(circle at top left, #e0ecff, #f5f5f7);
    }

    .hero-inner {
      display: grid;
      grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr);
      gap: 2rem;
      align-items: center;
    }

    .hero-title {
      font-size: clamp(2rem, 3vw + 1rem, 3rem);
      font-weight: 700;
      margin-bottom: 0.75rem;
    }

    .hero-subtitle {
      font-size: 1rem;
      color: #4b5563;
      margin-bottom: 1.5rem;
      max-width: 32rem;
    }

    .hero-actions {
      display: flex;
      flex-wrap: wrap;
      gap: 0.75rem;
      margin-bottom: 1.5rem;
    }

    .hero-note {
      font-size: 0.85rem;
      color: #6b7280;
    }

    .hero-panel {
      background: #fff;
      border-radius: 1.25rem;
      padding: 1.25rem;
      box-shadow: 0 18px 45px rgba(15, 23, 42, 0.08);
      border: 1px solid #e5e7eb;
    }

    .hero-panel-header {
      font-weight: 600;
      margin-bottom: 0.75rem;
    }

    .upload-box {
      border: 1px dashed #cbd5f5;
      background: #f9fbff;
      border-radius: 0.9rem;
      padding: 1.25rem;
      text-align: center;
      font-size: 0.9rem;
      color: #6b7280;
      margin-bottom: 1rem;
    }

    .upload-box strong {
      display: block;
      margin-bottom: 0.35rem;
      color: #111827;
    }

    .result-lines {
      font-family: "SF Mono", Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      font-size: 0.8rem;
      background: #0b1020;
      color: #e5e7eb;
      border-radius: 0.75rem;
      padding: 0.9rem;
      overflow-x: auto;
    }

    /* ---- Sections ---- */
    section {
      padding: 2.5rem 0;
    }

    .section-title {
      font-size: 1.5rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      text-align: center;
    }

    .section-subtitle {
      max-width: 34rem;
      margin: 0 auto 1.75rem;
      text-align: center;
      color: #6b7280;
      font-size: 0.95rem;
    }

    /* ---- Use cases / cards ---- */
    .grid {
      display: grid;
      gap: 1.25rem;
    }

    @media (min-width: 700px) {
      .grid-3 {
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }
      .grid-2 {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    .card {
      background: #ffffff;
      border-radius: 1rem;
      padding: 1.25rem;
      border: 1px solid #e5e7eb;
      box-shadow: 0 8px 18px rgba(15, 23, 42, 0.03);
    }

    .card h3 {
      font-size: 1rem;
      margin-top: 0;
      margin-bottom: 0.4rem;
    }

    .card p {
      font-size: 0.9rem;
      color: #4b5563;
      margin: 0;
    }

    .badge {
      display: inline-flex;
      padding: 0.18rem 0.5rem;
      border-radius: 999px;
      font-size: 0.75rem;
      background: #eff6ff;
      color: #1d4ed8;
      margin-bottom: 0.5rem;
    }

    /* ---- API section ---- */
    .code-block {
      font-family: "SF Mono", Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      background: #0b1020;
      color: #e5e7eb;
      border-radius: 0.75rem;
      padding: 1rem;
      font-size: 0.85rem;
      overflow-x: auto;
      margin-top: 1.25rem;
    }

    /* ---- CTA ---- */
    .cta {
      text-align: center;
      padding: 2.5rem 1.5rem;
      background: linear-gradient(135deg, #2563eb, #1e40af);
      color: #ffffff;
      border-radius: 1.5rem;
      margin-bottom: 2.5rem;
    }

    .cta h2 {
      margin-top: 0;
      margin-bottom: 0.5rem;
      font-size: 1.6rem;
    }

    .cta p {
      margin-top: 0;
      margin-bottom: 1.25rem;
      font-size: 0.98rem;
      opacity: 0.9;
    }

    /* ---- Footer ---- */
    footer {
      border-top: 1px solid #e5e7eb;
      padding: 1.5rem 0 2rem;
      font-size: 0.85rem;
      color: #6b7280;
      background: #ffffff;
    }

    .footer-inner {
      display: flex;
      justify-content: space-between;
      gap: 1.5rem;
      flex-wrap: wrap;
    }

    .footer-links {
      display: flex;
      gap: 0.9rem;
      flex-wrap: wrap;
    }
  </style>
</head>
<body>

  <!-- Header / Navigation -->
  <header>
    <div class="container nav">
      <div class="logo">Your<span>Project</span></div>

      <nav class="nav-links">
        <a href="#hero">Home</a>
        <a href="#use-cases">Use cases</a>
        <a href="#features">Features</a>
        <a href="#api">API</a>
        <a href="#contact">Contact</a>
      </nav>

      <div class="nav-actions">
        <button class="btn-outline btn" type="button">Log in</button>
        <button class="btn" type="button">Sign up</button>
      </div>
    </div>
  </header>

  <!-- Hero / Upload area -->
  <main>
    <section class="hero" id="hero">
      <div class="container hero-inner">
        <div>
          <h1 class="hero-title">Detect AI-generated images in seconds.</h1>
          <p class="hero-subtitle">
            Replace this text with a short explanation of what your tool does.
            For example: “Upload any image and we’ll estimate whether it was generated by AI.”
          </p>

          <div class="hero-actions">
            <button class="btn" type="button">Check an image</button>
            <button class="btn-outline btn" type="button">View API docs</button>
          </div>

          <p class="hero-note">
            No credit card required. Free trial available.
          </p>
        </div>

        <aside class="hero-panel" aria-label="Upload and sample result">
          <div class="hero-panel-header">Try it now</div>

          <div class="upload-box">
            <!-- Replace this with your real upload control / form -->
            <strong>Drop an image here</strong>
            or click to browse from your device.
          </div>

          <div class="result-lines">
<pre>// Sample JSON-style result (static example)
{
  "status": "success",
  "ai_generated": 0.92,
  "label": "Likely AI-generated"
}</pre>
          </div>
        </aside>
      </div>
    </section>

    <!-- Use Cases -->
    <section id="use-cases">
      <div class="container">
        <h2 class="section-title">What you can detect</h2>
        <p class="section-subtitle">
          Describe the main problems your project solves. These are just examples –
          rename or remove cards to match your product.
        </p>

        <div class="grid grid-3">
          <article class="card">
            <div class="badge">Misinformation</div>
            <h3>Misleading visuals</h3>
            <p>Spot AI-generated photos pretending to show real events or news.</p>
          </article>

          <article class="card">
            <div class="badge">Fraud</div>
            <h3>Fake documents</h3>
            <p>Detect synthetic IDs, invoices, or claims created with AI tools.</p>
          </article>

          <article class="card">
            <div class="badge">Safety</div>
            <h3>Fake profiles</h3>
            <p>Flag profile photos that are likely generated by AI instead of real users.</p>
          </article>

          <article class="card">
            <div class="badge">Moderation</div>
            <h3>User-generated content</h3>
            <p>Scan uploads on your platform to keep spam and abuse in check.</p>
          </article>

          <article class="card">
            <div class="badge">Compliance</div>
            <h3>KYC / onboarding</h3>
            <p>Help prevent AI-generated selfies from bypassing your onboarding checks.</p>
          </article>

          <article class="card">
            <div class="badge">Custom</div>
            <h3>Your use case here</h3>
            <p>Replace with your own scenario – e-commerce, education, internal tooling, etc.</p>
          </article>
        </div>
      </div>
    </section>

    <!-- Key Features -->
    <section id="features">
      <div class="container">
        <h2 class="section-title">Why teams use YourProject</h2>
        <p class="section-subtitle">
          A simpler version of a “feature list” section. Edit the text to match what
          your system actually offers.
        </p>

        <div class="grid grid-3">
          <article class="card">
            <h3>Accurate scores</h3>
            <p>Return a numeric confidence score instead of just “yes or no”, so you can
               choose your own thresholds.</p>
          </article>

          <article class="card">
            <h3>Easy integration</h3>
            <p>Use a single REST endpoint with simple JSON responses. Works with any backend stack.</p>
          </article>

          <article class="card">
            <h3>Scales with you</h3>
            <p>Start with manual uploads and graduate to API-based checks for large volumes.</p>
          </article>

          <article class="card">
            <h3>Privacy-conscious</h3>
            <p>Explain here how you handle user data, retention, or on-prem options if relevant.</p>
          </article>

          <article class="card">
            <h3>Dashboard (optional)</h3>
            <p>Mention any dashboard, analytics, or visualization features you plan to offer.</p>
          </article>

          <article class="card">
            <h3>Custom rules</h3>
            <p>Let customers decide what counts as “suspicious” using adjustable thresholds.</p>
          </article>
        </div>
      </div>
    </section>

    <!-- API Example -->
    <section id="api">
      <div class="container">
        <h2 class="section-title">Use it via API</h2>
        <p class="section-subtitle">
          Show a simple example so developers know how to get started.
          Change the URL, parameters, and response to match your real API.
        </p>

        <div class="grid grid-2">
          <div>
            <h3>Simple HTTP request</h3>
            <p style="font-size: 0.95rem; color: #4b5563;">
              Call a single endpoint with your API key and the image URL or file.
              Receive a JSON response including a score for how likely the image is AI-generated.
            </p>

            <div class="code-block">
<pre>curl -X POST "https://api.yourproject.com/v1/check" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -F "image=@/path/to/image.jpg" \
  -F "models=ai_image_detection"</pre>
            </div>
          </div>

          <div>
            <h3>Example JSON response</h3>
            <div class="code-block">
<pre>{
  "id": "req_123456",
  "status": "ok",
  "result": {
    "ai_generated": 0.92,
    "label": "likely_ai",
    "source": "diffusion"
  }
}</pre>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Call to action / Contact -->
    <section id="contact">
      <div class="container">
        <div class="cta">
          <h2>Ready to integrate AI image detection?</h2>
          <p>
            Short pitch goes here. For example: “Create a free account, get an API key in under a minute,
            and start testing on your own images.”
          </p>
          <button class="btn" type="button" style="background:#ffffff;color:#1d4ed8;">
            Get started
          </button>
        </div>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer>
    <div class="container footer-inner">
      <div>© <span id="year"></span> YourProject. All rights reserved.</div>
      <div class="footer-links">
        <a href="#">Docs</a>
        <a href="#">Status</a>
        <a href="#">Privacy</a>
        <a href="#">Terms</a>
      </div>
    </div>
  </footer>

  <script>
    // Auto-update footer year
    document.getElementById("year").textContent = new Date().getFullYear();
  </script>
</body>
</html>
